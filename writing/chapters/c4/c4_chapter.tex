\chapter{Evaluation Methodology}
\label{chap:evaluation-methodology}

This chapter establishes the experimental setting that makes the results in
Chapter~\ref{chap:experiment-results} interpretable. It describes the student
and teacher models, the three VQA benchmarks and their evaluation metrics,
the baselines that \methodname{} is compared against and the ablation
variants used to isolate each component's contribution. Readers who want to
reproduce or extend the experiments will find the complete hyperparameter
values in Appendix~\ref{app:experiment-parameters}.

\section{Experimental Setup}
The student models are \textbf{SmolVLM-500M} and \textbf{SmolVLM-256M}
\cite{marafioti2025smolvlm}, evaluated with \textbf{VLM-Eval-Kit}
\cite{duan2024vlmevalkit}, which provides a unified inference and evaluation pipeline across all three
benchmarks. For each sample, the toolkit loads the image-question pair, applies
dataset-specific prompts and runs the model to produce a text answer.

Before training, all benchmark samples are converted into a unified
\textbf{LLaVA-style multimodal instruction format}
\cite{liu2023visualinstructiontuning} as shown in listing~\ref{lst:llava-format},
so all three benchmarks share the same data loader and training pipeline.

\Needspace{12\baselineskip}
\begin{lstlisting}[basicstyle=\ttfamily\footnotesize,breaklines=true,frame=single,caption={Representative LLaVA-style sample used after dataset conversion.},label={lst:llava-format}]
{
  "image": "sample_000123.jpg",
  "conversations": [
    {"from": "human", "value": "<image>\nWhat is the invoice number?"},
    {"from": "gpt", "value": "4572"}
  ]
}
\end{lstlisting}

We run all training jobs on a single high-memory \textbf{NVIDIA H200} GPU or
a comparable single-GPU \textbf{Vast.ai} instance. Distillation training uses \textbf{DeepSpeed ZeRO Stage 2},
\textbf{bf16} mixed precision, \textbf{TF32} matrix multiplication and
gradient checkpointing to control memory, keeping the student trainable and
all teachers frozen
\cite{rajbhandari2020zero,chen2016sublinear}. Full optimization and routing
parameter values are reported in Appendix~\ref{app:experiment-parameters},
Table~\ref{tab:experiment-parameters}. Each configuration was run once because the per-run compute cost made multiple seeds impractical, with implications discussed in the Conclusion. Teacher logit storage and remote caching are in
Appendix~\ref{app:offline-teacher-logit-preparation} and
Appendix~\ref{app:remote-logit-streaming}.

\section{Datasets}
We evaluate three text-centric VQA benchmarks: \textbf{TextVQA}, \textbf{DocVQA} and \textbf{ChartQA}. Together they span OCR-heavy, layout-based and numerical visual reasoning \cite{singh2019vqamodelsread,mathew2021docvqadatasetvqadocument,masry2022chartqabenchmarkquestionanswering}.

\textbf{TextVQA} tests question answering on natural images containing scene
text \cite{singh2019vqamodelsread}.
\textit{Sample question}: ``What word is written on the stop sign?''

\textbf{DocVQA} tests question answering on scanned document images, where
layout, paragraphs, tables and forms determine the answer
\cite{mathew2021docvqadatasetvqadocument}.
\textit{Sample question}: ``What is the invoice number shown in this document?''

\textbf{ChartQA} tests question answering over charts, requiring numerical
and logical reasoning \cite{masry2022chartqabenchmarkquestionanswering}.
\textit{Sample question}: ``Which year has the highest sales value in the chart?''

No single benchmark captures all three and a method that excels at only one would still fail on the others. Figure~\ref{fig:dataset-samples} shows representative samples from each dataset. The TextVQA panel shows a stop sign and the answer requires reading text embedded
in the scene. The DocVQA panel shows a receipt with fields, item rows and totals. The ChartQA panel shows a bar chart and
answering requires comparing bar heights and reading values from the plot. Table~\ref{tab:dataset-sizes}
lists the split sizes.

\stepcounter{footnote}
\begin{figure}
    \centering
    \begin{subfigure}{0.31\linewidth}
        \centering
        \includegraphics[width=\linewidth,height=0.18\textheight,keepaspectratio]{figures/textvqa_sample_stop_sign.jpg}
        \caption{TextVQA}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.31\linewidth}
        \centering
        \includegraphics[width=\linewidth,height=0.18\textheight,keepaspectratio]{figures/docvqa_sample_receipt.jpg}
        \caption{DocVQA}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.31\linewidth}
        \centering
        \includegraphics[width=\linewidth,height=0.18\textheight,keepaspectratio]{figures/chartqa_sample_bar_chart.png}
        \caption{ChartQA}
    \end{subfigure}
    \caption[Representative dataset examples.]{Representative dataset examples.\textsuperscript{\thefootnote}}
    \label{fig:dataset-samples}
\end{figure}
\footnotetext[\value{footnote}]{Images adapted from Wikimedia Commons sources~\cite{wikimedia_stop_sign_image,wikimedia_receipt_swiss_image,wikimedia_bar_chart_image}.}


\begin{table}[H]
\centering
\caption[Dataset split sizes.]{Dataset split sizes.}
\label{tab:dataset-sizes}
\scalebox{0.96}{
\begin{tabular}{lrrrr}
\toprule
\textbf{Dataset} & \textbf{Train} & \textbf{Val} & \textbf{Test} & \textbf{Total} \\
\midrule
TextVQA & 34,602 & 5,000 & 5,734 & 45,336 \\
DocVQA & 39,463 & 5,349 & 5,188 & 50,000 \\
ChartQA-H (human) & 7,398 & 960 & 1,250 & 9,608 \\
ChartQA-M (machine) & 20,901 & 960 & 1,250 & 23,111 \\
ChartQA (H+M, combined) & 28,299 & 1,920 & 2,500 & 32,719 \\
\bottomrule
\end{tabular}
}
\end{table}

Each benchmark uses a distinct metric: TextVQA uses \textbf{VQA Accuracy},
DocVQA uses \textbf{ANLS} and ChartQA uses \textbf{Relaxed Accuracy}. For TextVQA, the per-question
score for a predicted answer \(\hat{a}\) is
\(s_{\text{VQA}}=\min(\#\text{annotators that provided }\hat{a}/3,1)\) and
the final score is the mean over all questions \cite{singh2019vqamodelsread}.
For DocVQA, the metric is \cite{mathew2021docvqadatasetvqadocument}:
\[
\mathrm{NLS}(a,\hat{a})=1-\frac{d_{\text{Lev}}(a,\hat{a})}{\max(|a|,|\hat{a}|)}, \quad
s_{\text{ANLS}}=
\begin{cases}
\mathrm{NLS}(a,\hat{a}), & \mathrm{NLS}\ge 0.5,\\
0, & \text{otherwise}.
\end{cases}
\]
For ChartQA, we report Relaxed Accuracy
\cite{masry2022chartqabenchmarkquestionanswering}. Text predictions must match
exactly, while numeric predictions are scored with
\[
s_{\mathrm{ChartQA}}=
\begin{cases}
\mathbf{1}[\hat{y}=y], & y=0,\\
\mathbf{1}\!\left[\frac{|\hat{y}-y|}{|y|}\le 0.05\right], & y\ne0.
\end{cases}
\]

\section{Baselines}
\label{sec:baselines}

Table~\ref{tab:baseline-groups} organizes the compared methods into four
groups: non-distillation references, same-family KD, single-teacher transfer with mismatched tokenizers and heterogeneous teacher methods.The teacher setup column references SmolVLM2
\cite{smolvlm2_modelcard,marafioti2025smolvlm}, Qwen2-VL
\cite{qwen2vl2b_modelcard,wang2024qwen2vl}, Qwen2.5-VL
\cite{qwen25vl3b_modelcard,bai2025qwen25vl}, InternVL3
\cite{internvl3_blog,internvl3_1b_modelcard} and Gemma 3
\cite{gemma3_technical_report}.


\begin{table}[tp]
\centering
\caption[Baseline groups.]{Baseline groups.}
\label{tab:baseline-groups}
\footnotesize
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.25}
\begin{tabularx}{\linewidth}{@{}
  >{\raggedright\arraybackslash}p{0.17\linewidth}
  >{\raggedright\arraybackslash}X
  >{\raggedright\arraybackslash}p{0.25\linewidth}
  >{\raggedright\arraybackslash}X@{}}
\toprule
\textbf{Group} & \textbf{Methods} & \textbf{Teacher setup} & \textbf{Purpose} \\
\midrule
Non-distillation
  & Original, full fine-tuning
  & No teacher
  & Student reference points \\[3pt]
Same-family
  & Forward KL, Reverse KL, Jensen-Shannon
  & \mbox{SmolVLM2-2.2B-Instruct}
  & Single-teacher KL objectives within the SmolVLM family \\[3pt]
Cross-family single-teacher
  & ULD
  & \mbox{Qwen2-VL-2B-Instruct}
  & Cross-family transfer \\[3pt]
Heterogeneous teacher baselines
  & Uniform averaging, SHM-CKA, REINFORCE, \textbf{\methodname{} (Ours)}
  & Mixed set from multiple VLM families
  & Compares static averaging, hidden-state matching and adaptive assignment \\
\bottomrule
\end{tabularx}
\end{table}


The same-family baselines use \textbf{SmolVLM2-2.2B-Instruct} as teacher for
forward KL, reverse KL and Jensen-Shannon distillation, testing aligned-vocabulary
transfer before any tokenizer mismatch is introduced
\cite{hinton2015distillingknowledgeneuralnetwork,lin1991jensen,cui2024sinkhorn}.
The different-family single-teacher setting uses ULD with
\textbf{Qwen2-VL-2B-Instruct} outside the SmolVLM family, testing transfer across mismatched tokenizers with one
teacher \cite{boizard2024towardscrosstokenizerdistillation}. The heterogeneous
teacher setting compares uniform averaging, SHM-CKA, REINFORCE and
\methodname{} over a mixed teacher pool, separating static averaging,
hidden-state matching and adaptive teacher assignment
\cite{takezawa2021supervisedtreewasserstein,zhang2023adaptivemultiteachermetalearning,yu2024selectdistill,dasgupta2025improving}. All trainable competitor methods retain the supervised term
\(\mathcal{L}_{CE}\), so their differences come from the added transfer signal:
the logits-only baselines change only the KD term, SHM-CKA adds hidden-state
alignment and REINFORCE adds a policy-gradient term
\cite{kornblith2019similarityrepresentations,williams1992simple,yuan2020reinforcedmultiteacherselection}.
In the descriptions below, \(\gamma\) denotes a generic KD mixing coefficient,
while \(\alpha_{\mathrm{KD}}\) is reserved for the \methodname{} objective in
Equation~\ref{eq:final-objective}.

\textbf{Original.} Direct evaluation of the released student, with no additional
optimization. This measures the native capability of the compact model before
any task adaptation or distillation.

\textbf{Full fine-tuning.} Uses \(\mathcal{L}=\mathcal{L}_{CE}\) with no
teacher, so it isolates the benefit of external supervision alone.

\textbf{Forward KL.} Uses
\(\mathcal{L}_{KD}=\mathrm{KL}(p_t\|p_s)\) for same-family soft-target
distillation when teacher and student vocabularies are aligned
\cite{hinton2015distillingknowledgeneuralnetwork}.

\textbf{Reverse KL.} Uses \(\mathcal{L}_{KD}=\mathrm{KL}(p_s\|p_t)\)
\cite{cui2024sinkhorn,wen2023fdistill} as a student-centered divergence
baseline, which tests whether matching the teacher from the student's current
distribution is suitable for VLM distillation. Because the reverse-KL gradient
is weighted by \(p_s\), this objective tends to \textbf{emphasize regions where the
student already assigns probability}.

\textbf{Jensen-Shannon.} Uses the symmetric divergence
\[
\frac{1}{2}\mathrm{KL}(p_s\|m)+\frac{1}{2}\mathrm{KL}(p_t\|m),
\qquad
m=\frac{1}{2}(p_s+p_t).
\]
Jensen-Shannon tests whether a bounded symmetric objective is more stable than KL
\cite{lin1991jensen}.

\textbf{ULD.} Uses \(\mathcal{L}_{KD}=\|\pi(p_s)-\pi(p_t)\|_1\)
\cite{boizard2024towardscrosstokenizerdistillation}, where \(\pi(\cdot)\)
sorts the softened probability vector in descending order and pads unequal
vocabulary sizes before comparison.

\textbf{SHM-CKA.} Extends ULD with a hidden-state CKA penalty
\cite{boizard2024towardscrosstokenizerdistillation,dasgupta2025improving,kornblith2019similarityrepresentations}.
For the heterogeneous setting, both the ULD and layer-matching terms are
averaged uniformly across teachers before combining with \(\mathcal{L}_{CE}\).
Full implementation details are given in
Appendix~\ref{app:baseline-implementation-details}.

\textbf{Uniform multi-teacher.} Averages the heterogeneous teacher losses with
fixed equal weights,
\(\mathcal{L}_{KD}=\frac{1}{M}\sum_{i=1}^{M}\mathcal{L}_{KD}^{(i)}\)
\cite{yuan2020reinforcedmultiteacherselection,zhang2023adaptivemultiteachermetalearning},
isolating the effect of teacher diversity from any adaptive assignment.

\textbf{REINFORCE.} Replaces deterministic routing with a stochastic policy
that samples teachers and updates the selector with REINFORCE
\cite{williams1992simple,yuan2020reinforcedmultiteacherselection}, using
policy-gradient selection in place of gradient-agreement scoring. The selector objective is
\begin{equation}
\mathcal{L}_{\mathrm{pol}}
=
-(r-b)\log p(\mathbf{m}\mid\boldsymbol{\pi}),
\end{equation}
where \(r\) is the reward, \(b\) is an EMA reward baseline and
\(\mathbf{m}\) is the sampled teacher mask. Its final training objective is
\begin{equation}
\mathcal{L}_{\mathrm{RS}}
=
\mathcal{L}_{CE}
+ \gamma \mathcal{L}_{KD}
+ \lambda_{\pi}\mathcal{L}_{\mathrm{pol}}.
\end{equation}
Additional baseline implementation details are in
Appendix~\ref{app:baseline-implementation-details}.

Table~\ref{tab:all-results} reports pre-distillation scores for both student
models and each teacher across the three benchmarks, evaluated using
\textbf{VLM-Eval-Kit} \cite{duan2024vlmevalkit}. These scores serve as
reference points for computing teacher-student gaps in
Chapter~\ref{chap:experiment-results}.

\begin{table}[H]
\centering
\caption[Student and teacher model results.]{Student and teacher model results.}
\label{tab:all-results}
\begin{tabular}{lccc}
\toprule
\textbf{Model} & \textbf{TextVQA} & \textbf{DocVQA} & \textbf{ChartQA} \\
\midrule
\multicolumn{4}{l}{\textit{Student Models}} \\
\midrule
SmolVLM-500M & 56.75 & 59.52 & 62.80 \\
SmolVLM-256M & 47.17 & 48.64 & 45.04 \\
\midrule
\multicolumn{4}{l}{\textit{Teacher Models}} \\
\midrule
Qwen2.5-VL-3B-Instruct & 79.30 & 93.90 & 94.24 \\
InternVL3-1B & 74.10 & 81.90 & 75.30 \\
Gemma-3-4B-it & 57.80 & 75.80 & 63.60 \\
Qwen2-VL-2B-Instruct & 79.70 & 90.10 & 89.28 \\
\bottomrule
\end{tabular}
\end{table}

Model sources are SmolVLM \cite{marafioti2025smolvlm}, Qwen2.5-VL
\cite{qwen25vl3b_modelcard,bai2025qwen25vl}, InternVL3
\cite{internvl3_blog}, Gemma~3 \cite{gemma3_technical_report} and Qwen2-VL
\cite{qwen2vl2b_modelcard,wang2024qwen2vl}. The scores reveal meaningful
specialization: Qwen2-VL leads on TextVQA (79.70) and is competitive on
DocVQA (90.10), while Qwen2.5-VL achieves the highest scores on both DocVQA
(93.90) and ChartQA (94.24). InternVL3-1B contributes competitive but not
top scores across all three benchmarks. Gemma-3-4B-it is the weakest
teacher, particularly on TextVQA (57.80), where it barely exceeds the
original student baseline. \textbf{No single teacher dominates all three
benchmarks}, motivating the per-sample routing approach described in
Chapter~\ref{chap:proposed-method}.


\section{Ablation Settings}
\label{sec:ablation-settings}

The ablation study in Section~\ref{sec:ablation} isolates the main components
of \methodname{} on DocVQA with all other settings fixed. Each variant changes
one mechanism, separating the contributions of teacher diversity, sparse
routing, gradient-agreement weighting and EMA score smoothing.

\textbf{Cross-family ULD.} Uses one teacher outside the SmolVLM family:
\[
\mathcal{L}
=
\mathcal{L}_{CE}
+
\alpha_{\mathrm{KD}}\mathcal{L}_{ULD},
\qquad
\mathcal{L}_{ULD}
=
\|\pi(p_s)-\pi(p_t)\|_1 .
\]

\textbf{Uniform averaging.} Adds the full heterogeneous teacher pool with fixed
equal weights, isolating teacher diversity from adaptive weighting:
\[
\mathcal{L}
=
\mathcal{L}_{CE}
+
\alpha_{\mathrm{KD}}\frac{1}{M}\sum_{i=1}^{M}\mathcal{L}_{TW}^{(i)}.
\]

\textbf{Routing only.} Keeps sparse teacher selection but replaces router
weights with uniform weights within the routed set, testing top-\(k\) routing
without gradient-agreement refinement:
\[
w_i^{(b)}
=
u_i^{(b)}
=
\frac{\mathbf{1}[i\in\mathcal{R}_b]}{|\mathcal{R}_b|}.
\]

\textbf{Full GRACE.} Combines trie-Wasserstein KD, sparse routing and
gradient-agreement weighting:
\[
w_i^{(b)}
\propto
(\hat p_i^{(b)})^{\lambda_{\mathrm{blend}}}
(\omega_i^{(b)})^{1-\lambda_{\mathrm{blend}}}.
\]
Two additional ablation variants isolate the final weighting mechanism,
reported in Table~\ref{tab:docvqa-planned-ablation}. The
\(\lambda_{\mathrm{blend}}=1.0\) variant keeps the threshold on the active set and
fallback rules but removes the softmax over agreement scores from the
high-confidence case:
\[
w_i^{(b)}
\propto
\hat{p}_i^{(b)}\mathbf{1}[i\in\mathcal{A}_b].
\]
Unlike Routing only, which weights all routed teachers, this variant restricts
weights to the active set \(\mathcal{A}_b\). The
\(\beta_{\mathrm{EMA}}=0\)
variant removes temporal smoothing by setting \(\hat{s}_i=s_i\) before
softmax weighting over agreement scores.
