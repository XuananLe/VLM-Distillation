\chapter{Experiment Results}
\label{chap:experiment-results}



\section{Main Comparison Results}

This section reports the main empirical comparison used to answer
RQ1. The results are organized from simpler reference points to the full
heterogeneous multi-teacher setting: original students, supervised
fine-tuning, same-family logits KD, cross-family ULD and multi-teacher
distillation methods. The section first establishes the student and teacher
reference ranges, then compares all methods on TextVQA, DocVQA and ChartQA.
This ordering separates
two questions: whether distillation improves over the student-only reference
point and whether adaptive multi-teacher weighting improves over fixed or
single-teacher transfer.

\subsection{Baseline Results}
Before applying adaptation or distillation, we report the original performance
of the two student models, \textbf{SmolVLM-500M} and
\textbf{SmolVLM-256M}, using \textbf{VLM-Eval-Kit}
\cite{duan2024vlmevalkit} under the evaluation pipeline described in
Chapter~\ref{chap:evaluation-methodology}. The student-only starting point for
all later fine-tuning and distillation comparisons is given in
Table~\ref{tab:smolvlm-original-results}. The teacher-side reference range is
reported in Table~\ref{tab:teacher-original-results}. The gap between these
student models and the stronger teachers motivates the distillation
experiments that follow.

\begin{table}[H]
\centering
\caption[Original SmolVLM student results.]{Original SmolVLM student results.}
\label{tab:smolvlm-original-results}
\begin{tabular}{lccc}
\toprule
\textbf{Model} & \textbf{TextVQA} & \textbf{DocVQA} & \textbf{ChartQA} \\
\midrule
SmolVLM-500M & 56.75 & 59.52 & 62.80 \\
SmolVLM-256M & 47.17 & 48.64 & 45.04 \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[H]
\centering
\caption[Teacher and reference-model results.]{Teacher and reference-model results.}
\label{tab:teacher-original-results}
\begin{tabular}{lccc}
\toprule
\textbf{Teacher Model} & \textbf{TextVQA} & \textbf{DocVQA} & \textbf{ChartQA} \\
\midrule
Qwen2.5-VL-3B-Instruct & 79.30 & 93.90 & 94.24 \\
InternVL3-1B & 74.10 & 81.90 & 75.30 \\
Gemma-3-4B-it & 57.80 & 75.80 & 63.60 \\
Qwen2-VL-2B-Instruct & 79.70 & 90.10 & 89.28 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Overall Comparison}

The main comparison results across original models, direct fine-tuning,
standard logits distillation baselines and \methodname{} are collected in
Tables~\ref{tab:textvqa-comparison}, \ref{tab:docvqa-comparison} and
\ref{tab:chartqa-comparison}. The main comparison tables report the strongest
completed run for each method and benchmark.
Uniform, SHM-CKA, REINFORCE and \methodname{} use a four-teacher mixed set
spanning the Qwen, InternVL and Gemma families.
Chapter~\ref{chap:evaluation-methodology} documents the four-teacher
configuration used across all multi-teacher experiments. We report the
baselines in four groups. The non-distillation references are the
original student and full-finetuning runs. The same-family baselines use
\textbf{SmolVLM2-2.2B-Instruct} as teacher for forward KL, reverse KL and
Jensen-Shannon divergence
\cite{hinton2015distillingknowledgeneuralnetwork,xu2024surveyknowledgedistillationlarge,dasgupta2025improving}.
The different-family single-teacher setting uses ULD with a teacher outside
the SmolVLM family \cite{boizard2024towardscrosstokenizerdistillation}. The
multi-teacher baselines compare four-teacher Uniform averaging with adaptive
methods such as SHM-CKA, REINFORCE and \methodname{}
\cite{williams1992simple,yuan2020reinforcedmultiteacherselection,zhang2023adaptivemultiteachermetalearning,yu2024selectdistill}.


\begin{table}[H]
\centering
\caption[TextVQA main comparison.]{TextVQA main comparison.}
\label{tab:textvqa-comparison}
\scriptsize
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.0}
\begin{tabular}{@{}>{\raggedright\arraybackslash}p{1.45cm}
                >{\raggedright\arraybackslash}p{1.85cm}
                >{\raggedright\arraybackslash}p{3.9cm}
                >{\raggedright\arraybackslash}p{2.25cm}
                r@{}}
\toprule
\textbf{Category} & \textbf{Method} & \textbf{Teacher} & \textbf{Student} & \textbf{TextVQA} \\
\midrule
\multicolumn{5}{@{}l@{}}{\textit{Non-distillation references}} \\
\midrule
\makecell[l]{Original} & None & None & \makecell[l]{SmolVLM-500M\\SmolVLM-256M} & \makecell[r]{56.75\\47.17} \\
\addlinespace[1pt]
\makecell[l]{Reference} & Finetuning & None & \makecell[l]{SmolVLM-500M\\SmolVLM-256M} & \makecell[r]{56.96\\49.83} \\
\midrule
\multicolumn{5}{@{}l@{}}{\textit{Same-family distillation baselines}} \\
\midrule
& Forward KL & & \makecell[l]{SmolVLM-500M\\SmolVLM-256M} & \makecell[r]{59.00\\50.88} \\
\addlinespace[1pt]
Same-family & Reverse KL & SmolVLM2-2.2B & \makecell[l]{SmolVLM-500M\\SmolVLM-256M} & \makecell[r]{44.73\\29.75} \\
\addlinespace[1pt]
& Jensen-Shannon & & \makecell[l]{SmolVLM-500M\\SmolVLM-256M} & \makecell[r]{42.33\\33.84} \\
\midrule
\multicolumn{5}{@{}l@{}}{\textit{Different-family single-teacher baselines}} \\
\midrule
Cross-family & ULD & Qwen2-VL-2B-Instruct & \makecell[l]{SmolVLM-500M\\SmolVLM-256M} & \makecell[r]{61.49\\53.95} \\
\midrule
\multicolumn{5}{@{}l@{}}{\textit{Multi-teacher baselines}} \\
\midrule
& \multirow[c]{2}{=}{Uniform}
& 
& SmolVLM-500M & 63.88 \\
& 
& 
& SmolVLM-256M & 54.57 \\

& \multirow[c]{2}{=}{SHM-CKA}
& \multirow[c]{4}{=}{\makecell[l]{Qwen2-VL-2B-Instruct\\Qwen2.5-VL-3B-Instruct\\InternVL3-1B\\Gemma-3-4B-it}}
& SmolVLM-500M & 60.47 \\
& 
& 
& SmolVLM-256M & 55.93 \\

& \multirow[c]{2}{=}{REINFORCE}
& 
& SmolVLM-500M & 63.26 \\
& 
& 
& SmolVLM-256M & 56.31 \\

& \multirow[c]{2}{=}{\makecell[l]{\textbf{\methodname{}}\\\textbf{(Ours)}}}
& 
& SmolVLM-500M & \textbf{69.38} \\
& 
& 
& SmolVLM-256M & \textbf{60.73} \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[H]
\centering
\caption[DocVQA main comparison.]{DocVQA main comparison.}
\label{tab:docvqa-comparison}
\scriptsize
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.0}
\begin{tabular}{@{}>{\raggedright\arraybackslash}p{1.45cm}
                >{\raggedright\arraybackslash}p{1.85cm}
                >{\raggedright\arraybackslash}p{3.9cm}
                >{\raggedright\arraybackslash}p{2.25cm}
                r@{}}
\toprule
\textbf{Category} & \textbf{Method} & \textbf{Teacher} & \textbf{Student} & \textbf{DocVQA} \\
\midrule
\multicolumn{5}{@{}l@{}}{\textit{Non-distillation references}} \\
\midrule
\makecell[l]{Original} & None & None & \makecell[l]{SmolVLM-500M\\SmolVLM-256M} & \makecell[r]{59.52\\48.64} \\
\addlinespace[1pt]
\makecell[l]{Reference} & Finetuning & None & \makecell[l]{SmolVLM-500M\\SmolVLM-256M} & \makecell[r]{61.96\\42.03} \\
\midrule

\multicolumn{5}{@{}l@{}}{\textit{Same-family distillation baselines}} \\
\midrule
& Forward KL & & \makecell[l]{SmolVLM-500M\\SmolVLM-256M} & \makecell[r]{63.55\\49.46} \\
\addlinespace[1pt]
Same-family & Reverse KL & SmolVLM2-2.2B & \makecell[l]{SmolVLM-500M\\SmolVLM-256M} & \makecell[r]{29.28\\23.28} \\
\addlinespace[1pt]
& Jensen-Shannon & & \makecell[l]{SmolVLM-500M\\SmolVLM-256M} & \makecell[r]{36.43\\33.12} \\
\midrule

\multicolumn{5}{@{}l@{}}{\textit{Different-family single-teacher baselines}} \\
\midrule
Cross-family & ULD & Qwen2-VL-2B-Instruct & \makecell[l]{SmolVLM-500M\\SmolVLM-256M} & \makecell[r]{65.28\\52.24} \\
\midrule

\multicolumn{5}{@{}l@{}}{\textit{Multi-teacher baselines}} \\
\midrule
& \multirow[c]{2}{=}{Uniform}
& 
& SmolVLM-500M & 69.59 \\
& 
& 
& SmolVLM-256M & 55.70 \\

& \multirow[c]{2}{=}{SHM-CKA}
& \multirow[c]{4}{=}{\makecell[l]{Qwen2-VL-2B-Instruct\\Qwen2.5-VL-3B-Instruct\\InternVL3-1B\\Gemma-3-4B-it}}
& SmolVLM-500M & 63.32 \\
& 
& 
& SmolVLM-256M & 57.24 \\

& \multirow[c]{2}{=}{REINFORCE}
& 
& SmolVLM-500M & 64.00 \\
& 
& 
& SmolVLM-256M & 58.53 \\

& \multirow[c]{2}{=}{\makecell[l]{\textbf{\methodname{}}\\\textbf{(Ours)}}}
& 
& SmolVLM-500M & \textbf{73.11} \\
& 
& 
& SmolVLM-256M & \textbf{60.48} \\
\bottomrule
\end{tabular}
\end{table}


\begin{table}[H]
\centering
\caption[ChartQA main comparison.]{ChartQA main comparison.}
\label{tab:chartqa-comparison}
\scriptsize
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.0}
\begin{tabular}{@{}>{\raggedright\arraybackslash}p{1.45cm}
                >{\raggedright\arraybackslash}p{1.85cm}
                >{\raggedright\arraybackslash}p{3.9cm}
                >{\raggedright\arraybackslash}p{2.25cm}
                r@{}}
\toprule
\textbf{Category} & \textbf{Method} & \textbf{Teacher} & \textbf{Student} & \textbf{ChartQA} \\
\midrule
\multicolumn{5}{@{}l@{}}{\textit{Non-distillation references}} \\
\midrule
\makecell[l]{Original} & None & None & \makecell[l]{SmolVLM-500M\\SmolVLM-256M} & \makecell[r]{62.80\\45.04} \\
\addlinespace[1pt]
\makecell[l]{Reference} & Finetuning & None & \makecell[l]{SmolVLM-500M\\SmolVLM-256M} & \makecell[r]{63.00\\49.68} \\
\midrule
\multicolumn{5}{@{}l@{}}{\textit{Same-family distillation baselines}} \\
\midrule
& Forward KL & & \makecell[l]{SmolVLM-500M\\SmolVLM-256M} & \makecell[r]{64.52\\56.80} \\
\addlinespace[1pt]
Same-family & Reverse KL & SmolVLM2-2.2B & \makecell[l]{SmolVLM-500M\\SmolVLM-256M} & \makecell[r]{30.16\\17.20} \\
\addlinespace[1pt]
& Jensen-Shannon & & \makecell[l]{SmolVLM-500M\\SmolVLM-256M} & \makecell[r]{43.75\\50.93} \\
\midrule
\multicolumn{5}{@{}l@{}}{\textit{Different-family single-teacher baselines}} \\
\midrule
Cross-family & ULD & Qwen2-VL-2B-Instruct & \makecell[l]{SmolVLM-500M\\SmolVLM-256M} & \makecell[r]{66.92\\60.96} \\
\midrule
\multicolumn{5}{@{}l@{}}{\textit{Multi-teacher baselines}} \\
\midrule
& \multirow[c]{2}{=}{Uniform}
& 
& SmolVLM-500M & 75.60 \\
& 
& 
& SmolVLM-256M & 63.68 \\

& \multirow[c]{2}{=}{SHM-CKA}
& \multirow[c]{4}{=}{\makecell[l]{Qwen2-VL-2B-Instruct\\Qwen2.5-VL-3B-Instruct\\InternVL3-1B\\Gemma-3-4B-it}}
& SmolVLM-500M & 76.88 \\
& 
& 
& SmolVLM-256M & 62.40 \\

& \multirow[c]{2}{=}{REINFORCE}
& 
& SmolVLM-500M & 70.27 \\
& 
& 
& SmolVLM-256M & 68.74 \\

& \multirow[c]{2}{=}{\makecell[l]{\textbf{\methodname{}}\\\textbf{(Ours)}}}
& 
& SmolVLM-500M & \textbf{80.20} \\
& 
& 
& SmolVLM-256M & \textbf{72.32} \\
\bottomrule
\end{tabular}
\end{table}

On DocVQA, \methodname{} rises from 69.59 to 73.11 on SmolVLM-500M and from
55.70 to 60.48 on SmolVLM-256M relative to the Uniform baseline. ChartQA shows
the largest margins over Uniform: 75.60 to 80.20 for SmolVLM-500M and 63.68 to
72.32 for SmolVLM-256M. Several trends are consistent across the three
benchmarks. Same-family logits baselines remain weak: forward KL gives only
small gains, reverse KL is consistently poor and Jensen-Shannon is unstable.
Cross-family ULD is already stronger than the same-family logits losses on all
three tasks, showing that tokenizer-mismatched distillation is more useful here
than vocabulary-matched KL. Once multiple teachers are added, Uniform averaging
gives another clear gain, which means teacher diversity itself is valuable. The
additional improvement from single-teacher ULD to \methodname{} comes from the
full recipe: trie-Wasserstein KD, sample-dependent sparse routing and
gradient-agreement refinement after routing. \methodname{} also improves on
the uniform multi-teacher baseline for both student sizes. The gain is moderate
on TextVQA and DocVQA, but larger on ChartQA, indicating that adaptive
weighting helps most when the task mixes visual reading, numerical reasoning
and teacher specialization across datasets.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{figures/c5_results_bars.png}
    \caption[Grouped benchmark comparison.]{Grouped benchmark comparison.}
    \label{fig:c5-results-bars}
\end{figure}

Figure~\ref{fig:c5-results-bars} summarizes the main ranking by showing that
\methodname{} remains above the strongest non-\methodname{} baseline across all
three benchmarks and both student sizes.

To provide qualitative evidence of attention improvement, we use DEX-AR
attribution maps \cite{bousselham2026dexar}. For token \(t\), layer \(l\) and
head \(h\), DEX-AR forms a gradient-based attribution map
\[
\mathbf{G}_{t}^{(l,h)}
=
\nabla_{\mathbf{A}^{(l,h)}} y_t \odot \mathbf{A}^{(l,h)},
\]
then keeps visually grounded heads with
\[
w^{(l,h)}=\max\!\big(S_{\mathrm{img}}^{(l,h)}-S_{\mathrm{text}}^{(l,h)},0\big),
\]
and aggregates
\[
\mathbf{H}_t=\sum_{l,h} w^{(l,h)} \mathbf{G}_{t}^{(l,h)},
\qquad
\mathbf{H}_{\mathrm{sent}}=\sum_t \delta_t \mathbf{H}_t .
\]
Here \(\delta_t=1/|\Omega_{\mathrm{ans}}|\) for answer tokens
\(t\in\Omega_{\mathrm{ans}}\) and \(\delta_t=0\) otherwise, so the
sentence-level map is the uniform average of token-level maps over the
generated answer span. \(\Omega_{\mathrm{ans}}\) is the set of generated answer
token positions.
We prefer DEX-AR over raw attention maps or generic saliency because the model
is autoregressive and the target is answer-token grounding, not generic image
saliency. Figure~\ref{fig:dexar-architecture} summarizes this attribution
pipeline before the qualitative TextVQA example.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.82\linewidth]{figures/dexar_architecture.jpg}
    \caption[DEX-AR overview.]{DEX-AR overview.}
    \label{fig:dexar-architecture}
\end{figure}

Figure~\ref{fig:textvqa-sample3-qualitative} gives a representative TextVQA
example. Sample 3 asks \emph{``who is this copyrighted by?''} The relevant
visual evidence is the copyright text on the lower-left border. The original
SmolVLM-500M assigns high attention to the \emph{Virgin} logo instead of this
region. \methodname{} shifts attention toward the copyright text, reducing this
error.

\begin{figure}[H]
    \centering
    \begin{subfigure}[t]{0.48\linewidth}
        \centering
        \includegraphics[width=\linewidth]{figures/input_image.png}
        \caption{Input image}
    \end{subfigure}

    \vspace{0.5em}

    \begin{subfigure}[t]{0.34\linewidth}
        \centering
        \includegraphics[width=\linewidth]{figures/original_smolvlm_500_heatmap.png}
        \caption{Original SmolVLM-500M}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.34\linewidth}
        \centering
        \includegraphics[width=\linewidth]{figures/grace_smolvlm_500_heatmap.png}
        \caption{\methodname{} SmolVLM-500M}
    \end{subfigure}
    \caption[Representative TextVQA attention example.]{Representative TextVQA attention example.}
    \label{fig:textvqa-sample3-qualitative}
\end{figure}

\begin{rqanswerbox}{Answer to RQ1}
\methodname{} improves over direct finetuning, same-family logits KD,
ULD and the compared heterogeneous multi-teacher baselines. The
ranking is clear: same-family logits baselines are weakest, cross-family ULD is
stronger, multi-teacher baselines improve further and \methodname{} is best
overall. Within the multi-teacher group, static
Uniform averaging already helps, but \methodname{} stays ahead of SHM-CKA,
REINFORCE and Uniform itself. This shows that teacher diversity
already helps, but the largest gains come from combining the trie-Wasserstein
loss with gradient-based alignment. Table~\ref{tab:grace-gap-to-teacher}
quantifies how much of the teacher-student gap is closed by \methodname{}.
\end{rqanswerbox}

\begin{figure}[H]
    \centering
    \begin{subfigure}[t]{0.48\linewidth}
        \centering
        \includegraphics[width=\linewidth]{figures/uniform.png}
        \caption{Uniform baseline}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.48\linewidth}
        \centering
        \includegraphics[width=\linewidth]{figures/GRACE.png}
        \caption{\methodname}
    \end{subfigure}
    \caption[Uniform and GRACE training curves.]{Uniform and GRACE training curves.}
    \label{fig:training-curve-comparison}
\end{figure}

Figure~\ref{fig:training-curve-comparison} shows that the \methodname{} run
produces a smoother and more stable training curve than Uniform on DocVQA. The
Uniform curve shows sharper oscillations, while the \methodname{} curve
decreases more steadily after the early training phase.

\begin{figure}[H]
    \centering
    \begin{subfigure}[t]{0.74\linewidth}
        \centering
        \includegraphics[width=\linewidth]{figures/router-collapse-without-GRACE.png}
        \caption{Without GRACE refinement}
    \end{subfigure}

    \vspace{0.6em}

    \begin{subfigure}[t]{0.74\linewidth}
        \centering
        \includegraphics[width=\linewidth]{figures/GRACE-non-collapse.png}
        \caption{With \methodname{}}
    \end{subfigure}
    \caption[Routed-teacher load examples.]{Routed-teacher load examples.}
    \label{fig:router-collapse-comparison}
\end{figure}

Figure~\ref{fig:router-collapse-comparison} contrasts collapsed and stable
routing behavior. Without the full \methodname{} recipe, the routed load
concentrates on one teacher, reducing teacher diversity. With \methodname{},
the load stays distributed across the teacher pool, so the student continues
to receive heterogeneous supervision. The benchmark gains in
Table~\ref{tab:grace-relative-gains} are consistent with this routing behavior.
Relative gain is computed as
\((\text{\methodname} - \text{baseline}) / \text{baseline}\).

\begin{table}[H]
\centering
\caption[Relative gains of \methodname{} on SmolVLM-500M.]{Relative gains of \methodname{} on SmolVLM-500M.}
\label{tab:grace-relative-gains}
\scriptsize
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.02}
\begin{tabular}{lcccc}
\toprule
\textbf{Benchmark} & \textbf{vs Original} & \textbf{vs Finetuning} & \textbf{vs Uniform} & \makecell[c]{\textbf{vs Best}\\\textbf{non-\methodname{}}} \\
\midrule
TextVQA & +12.63 (+22.26\%) & +12.42 (+21.80\%) & +5.50 (+8.61\%) & +5.38 (+8.41\%) \\
DocVQA & +13.59 (+22.83\%) & +11.15 (+18.00\%) & +3.52 (+5.06\%) & +3.52 (+5.06\%) \\
ChartQA & +17.40 (+27.71\%) & +17.20 (+27.30\%) & +4.60 (+6.08\%) & +3.32 (+4.32\%) \\
\bottomrule
\end{tabular}
\end{table}

The gains over the original SmolVLM-500M student are large: +22.26\% on
TextVQA, +22.83\% on DocVQA and +27.71\% on ChartQA. The gains over direct
finetuning are almost as large, at +21.80\%, +18.00\% and +27.30\%,
respectively. The gain over static uniform averaging is smaller, but it
remains consistent across all three tasks, isolating the value of adaptive
teacher weighting from the value of simply using more than one teacher. The
same trend holds for SmolVLM-256M, with relative gains of +28.75\%,
+24.34\% and +60.57\% on the three benchmarks. For the gap-to-teacher
analysis, let the reference point be the strongest teacher in the
\methodname{} comparison pool reported in Table~\ref{tab:teacher-original-results}. Let
\(g_{\mathrm{orig}} = y_T - y_{\mathrm{orig}}\) and
\(g_{\mathrm{grace}} = y_T - y_{\mathrm{grace}}\). The fraction of the gap
closed is \(1 - g_{\mathrm{grace}} / g_{\mathrm{orig}}\).

\begin{table}[H]
\centering
\caption[Gap-to-teacher analysis for SmolVLM-500M.]{Gap-to-teacher analysis for SmolVLM-500M.}
\label{tab:grace-gap-to-teacher}
\small
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.02}
\begin{tabular}{lcccc}
\toprule
\textbf{Benchmark} & \textbf{Best teacher} & \textbf{Original gap} & \textbf{GRACE gap} & \textbf{Gap closed} \\
\midrule
TextVQA & 79.70 & 22.95 & 10.32 & 55.03\% \\
DocVQA & 93.90 & 34.38 & 20.79 & 39.53\% \\
ChartQA & 94.24 & 31.44 & 14.04 & 55.34\% \\
\bottomrule
\end{tabular}
\end{table}

\textbf{The gain is not merely an artifact of comparison with weak baselines.}
On two benchmarks, \methodname{} removes about half of the original
teacher-student gap and even on DocVQA, where the teacher ceiling is highest,
it still closes almost 40\% of the gap.

\textbf{The distilled student can outperform individual teachers.}
On TextVQA, \methodname{} reaches 69.38 and exceeds Gemma-3-4B-it at 57.80 by
+11.58 points (+20.03\%). On ChartQA, it exceeds InternVL3-1B at 75.30 by
+4.90 points (+6.51\%) and Gemma-3-4B-it at 63.60 by +16.60 points (+26.10\%).
This shows that routed multi-teacher supervision can combine useful signals
that are not available from any single teacher alone.

\textbf{One negative result is also consistent: reverse KL is much weaker than
forward KL.} This can be explained by the different student-logit gradients.
Let \(p_s=\mathrm{softmax}(z_s)\) be the student distribution and let \(p_t\)
be the fixed teacher distribution. For forward KL,
\[
\mathcal{L}_{FKL}
=
\mathrm{KL}(p_t\|p_s)
=
\sum_v p_t(v)\log\frac{p_t(v)}{p_s(v)},
\qquad
\frac{\partial \mathcal{L}_{FKL}}{\partial z_s(v)}
=
p_s(v)-p_t(v).
\]
Thus, every teacher-supported token directly pulls the student distribution
toward the teacher's soft target
\cite{hinton2015distillingknowledgeneuralnetwork}. Reverse KL gives
\[
\mathcal{L}_{RKL}
=
\mathrm{KL}(p_s\|p_t)
=
\sum_v p_s(v)\log\frac{p_s(v)}{p_t(v)}.
\]
The student-logit gradient is
\[
\frac{\partial \mathcal{L}_{RKL}}{\partial z_s(v)}
=
p_s(v)
\left(
\log\frac{p_s(v)}{p_t(v)}
-
\mathcal{L}_{RKL}
\right).
\]
The key difference is the factor \(p_s(v)\). If \(p_t(v)>0\) but
\(p_s(v)\rightarrow 0\), then
\[
\frac{\partial \mathcal{L}_{FKL}}{\partial z_s(v)}
\rightarrow
-p_t(v),
\qquad
\frac{\partial \mathcal{L}_{RKL}}{\partial z_s(v)}
\rightarrow
0.
\]
Teacher-supported tokens that the student currently assigns low probability
therefore receive weaker correction under reverse KL. In finite training, this
student-weighted gradient can make reverse KL less useful for answer-token
distillation, especially when teacher distributions contain useful secondary
mass over OCR variants, numeric formats or plausible answer alternatives. We
interpret this as an optimization-bias explanation, not as a universal rule,
because recent LLM distillation work shows that the simple mode-seeking
interpretation of reverse KL does not always fully describe KD behavior
\cite{wu2025rethinking}.

\textbf{SHM-CKA alone is also not sufficient because representation
similarity is not the same as task-useful output supervision.} CKA measures
hidden-state geometry \cite{kornblith2019similarityrepresentations}, but
hidden-state similarity does not imply output equivalence:
\[
\mathrm{CKA}(H_s,H_t)\approx 1
\;\not\Rightarrow\;
p_s(\cdot\mid x)\approx p_t(\cdot\mid x).
\]
A simple linear-head example shows why. Suppose the teacher representation is
an orthogonal rotation of the student representation,
\(H_t=H_sR\) with \(R^\top R=I\). Linear CKA gives
\[
\mathrm{CKA}(H_s,H_t)
=
\frac{\|H_t^\top H_s\|_F^2}
{\|H_t^\top H_t\|_F\|H_s^\top H_s\|_F}
=
1.
\]
However, the answer logits can still differ:
\[
z_s = H_sW_s,
\qquad
z_t = H_tW_t = H_sRW_t.
\]
Unless \(W_s=RW_t\), the two models can have identical CKA but different
logits and different answer-token distributions.
This distinction matches the difference between representational similarity
and functional similarity discussed in prior work
\cite{klabunde2023similarity}. In heterogeneous VLM distillation, the final
answer distribution also depends on the language head, tokenizer, output
vocabulary and task-specific teacher behavior. Although SHM-CKA is competitive
on ChartQA, it remains weaker than \methodname{} on TextVQA and DocVQA, where
output-level tokenizer-agnostic matching and sample-dependent teacher weighting
are more important.



\section{Representation Similarity Analysis}

We use Centered Kernel Alignment (CKA) as an analysis tool to examine whether teacher and student layers
exhibit clear correspondence. For centered hidden-state matrices
\(\tilde{\mathbf{H}}_s\) and \(\tilde{\mathbf{H}}_t\), linear CKA is
\begin{equation}
\mathrm{CKA}(\tilde{\mathbf{H}}_s,\tilde{\mathbf{H}}_t)=
\frac{\left\|\tilde{\mathbf{H}}_t^{\top}\tilde{\mathbf{H}}_s\right\|_F^2}
{\left\|\tilde{\mathbf{H}}_t^{\top}\tilde{\mathbf{H}}_t\right\|_F
\left\|\tilde{\mathbf{H}}_s^{\top}\tilde{\mathbf{H}}_s\right\|_F}.
\end{equation}
Values closer to \(1\) indicate stronger representation similarity
\cite{kornblith2019similarityrepresentations}.
CKA is therefore used here as a representation-analysis tool, not as evidence
that two models implement the same answer function. A high CKA score indicates
similar hidden geometry on the evaluated samples, but it does not guarantee
similar answer-token probabilities after the language head.

The main question is whether strong internal similarity is enough to justify
layer-wise distillation. The results show that it is not, as the
figures and tables below confirm.

\begin{figure}[htbp]
    \centering
    \begin{subfigure}[t]{0.48\linewidth}
        \centering
        \includegraphics[width=\linewidth]{figures/cka_last_textvqa_qwen2vl2b_smolvlm500m.png}
        \caption{TextVQA}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.48\linewidth}
        \centering
        \includegraphics[width=\linewidth]{figures/cka_last_docvqa_qwen2vl2b_smolvlm500m.png}
        \caption{DocVQA}
    \end{subfigure}
    \caption[Final-layer CKA heatmaps.]{Final-layer CKA heatmaps.}
    \label{fig:exp-cka-multi-dataset}
\end{figure}

The final-layer maps show that the teacher and student are not severely
misaligned in representation space.

\begin{figure}[htbp]
    \centering
    \begin{subfigure}[t]{0.48\linewidth}
        \centering
        \includegraphics[width=\linewidth]{figures/cka_vision_textvqa_qwen2vl2b_smolvlm500m.png}
        \caption{TextVQA}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.48\linewidth}
        \centering
        \includegraphics[width=\linewidth]{figures/cka_vision_docvqa_qwen2vl2b_smolvlm500m.png}
        \caption{DocVQA}
    \end{subfigure}
    \caption[Vision-layer CKA heatmaps.]{Vision-layer CKA heatmaps.}
    \label{fig:exp-cka-vision-multi-dataset}
\end{figure}

Teacher and student can remain relatively close in both the language and vision
stacks, as Figures~\ref{fig:exp-cka-multi-dataset} and
\ref{fig:exp-cka-vision-multi-dataset} indicate. However, the benchmark
comparisons in Tables~\ref{tab:textvqa-comparison},
\ref{tab:docvqa-comparison} and \ref{tab:chartqa-comparison} still contain a
clear gap between the student models and stronger teachers. The layer-wise baseline also fails to
consistently outperform output-level distillation. This pattern supports the design choice in GRACE: output-level
tokenizer-agnostic KD and sample-dependent teacher weighting are more reliable
than representation matching alone, because high hidden-state similarity is not
a reliable signal that direct layer matching will produce better task transfer.

\section{Ablation Study}
\label{sec:ablation}
This section answers RQ2 from Chapter~\ref{chap:introduction} by isolating the
main components of \methodname{} on DocVQA. Section~\ref{sec:ablation-settings}
defines the ablation variants, with the corresponding results for both student
models reported in Table~\ref{tab:docvqa-planned-ablation}.

\begin{table}[H]
\centering
\caption[DocVQA component ablation.]{DocVQA component ablation.}
\label{tab:docvqa-planned-ablation}
\scriptsize
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{0.94}
\begin{tabular}{@{}lcc@{}}
\toprule
\textbf{DocVQA ablation variant} & \makecell[c]{\textbf{SmolVLM-}\\\textbf{256M}} & \makecell[c]{\textbf{SmolVLM-}\\\textbf{500M}} \\
\midrule
Cross-family ULD (single teacher) & 52.24 & 65.28 \\
Uniform averaging & 55.70 & 69.59 \\
Routing only + trie-Wasserstein & 57.45 & 63.23 \\
Full GRACE + trie-Wasserstein & 60.48 & 73.11 \\
\makecell[l]{Full GRACE + trie-Wasserstein\\(\(\lambda_{\mathrm{blend}}=1.0\))} & 57.97 & 70.45 \\
\makecell[l]{Full GRACE + trie-Wasserstein\\(\(\beta_{\mathrm{EMA}}=0\))} & 57.45 & 71.25 \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Teacher diversity is necessary but not sufficient because uniform
multi-teacher averaging adds useful supervision while still failing to resolve
tokenizer mismatch, teacher specialization and conflicting KD directions.}
Across the ablation results in Table~\ref{tab:docvqa-planned-ablation}, the
full method is the strongest setting for both student sizes. Moving from a
single cross-family teacher (ULD) to uniform four-teacher
averaging adds +3.46 on SmolVLM-256M and +4.31 on SmolVLM-500M. The gain
confirms that multiple heterogeneous teachers provide better supervision than
any single teacher, but uniform weighting leaves a further gap of +4.78/+3.52
to the full method.

\textbf{Routing without gradient refinement can hurt.}
On SmolVLM-500M, routing-only scoring (63.23) is \textit{worse} than uniform
averaging (69.59) by $-6.36$ points. The router selects teachers but assigns
them incorrect relative weights. Without GRACE, these wrong weights degrade
the result below a naive equal average. On SmolVLM-256M the pattern reverses:
routing-only (57.45) beats uniform (55.70) by +1.75. SmolVLM-500M, the larger
student, produces a sharper routing distribution that amplifies weighting
errors when uncorrected. SmolVLM-256M produces a flatter routing signal closer
to uniform, so routing-only is less damaging and even slightly beneficial.

\textbf{Gradient-agreement refinement acts as the critical corrective layer
because it adjusts routed teacher weights according to whether each teacher's
KD direction agrees with the supervised task objective.}
Full GRACE recovers +9.88 points over routing-only on SmolVLM-500M and +3.03
on SmolVLM-256M. Decomposing the 500M gain, the active-set router-weight
variant (\(\lambda_{\mathrm{blend}}\!=\!1.0\)) already recovers +7.22 points
(63.23~\(\to\)~70.45) by replacing uniform post-routing weights with learned
router weights while retaining the GRACE active-set filter. Full score-softmax
blending then adds another +2.66 points (70.45~\(\to\)~73.11). On
SmolVLM-256M, the active-set router-weight variant adds only +0.52 points
(57.45~\(\to\)~57.97), while full score-based blending adds +2.51 points
(57.97~\(\to\)~60.48). These results show that active-set filtering and
router weighting help, but agreement-score weighting is still needed for the
strongest final performance.

Removing EMA smoothing ($\beta_{\mathrm{EMA}}\!=\!0$) degrades both students
(256M: 60.48~$\to$~57.45, 500M: 73.11~$\to$~71.25), confirming that temporal
smoothing stabilizes score estimates but is secondary to the refinement step
itself.

\begin{rqanswerbox}{Answer to RQ2}
The ablation isolates three cumulative contributions: (1)~heterogeneous
teacher diversity (+3 to +4 pts over single-teacher ULD), (2)~sparse routing
structure (beneficial only when paired with refinement) and
(3)~gradient-agreement refinement, which adds +3 to +10~pts over
routing-only and is the single largest contributor. EMA smoothing provides
additional stabilization by reducing the effect of noisy batch-level
gradient-agreement scores before they are converted into final KD weights.
Without gradient refinement, routing can degrade performance below uniform
averaging.
\end{rqanswerbox}

\section{Efficiency Analysis}
\label{sec:efficiency-analysis}
This section addresses RQ3 from Chapter~\ref{chap:introduction}. Runtime,
training cost and storage overhead are analyzed using the H200 rental rate and
the observed Cloudflare R2 storage footprint.

\begin{rqanswerbox}{Answer to RQ3}
\methodname{} is mainly limited by GPU training and offline logit generation,
not object storage. A 7-hour H200 run costs about 14.41 USD, while the 1.04 TB
Cloudflare R2 logit store in Figure~\ref{fig:r2-storage} costs about
15.60 USD/month for storage plus 3.71 USD for billable Class B operations, or
about 19.31 USD/month before rounding. The practical efficiency bottleneck is
compute, not storage.
\end{rqanswerbox}

\section{Discussion}
The results show a clear ordering across method families. Same-family KL
baselines are weakest, single cross-family distillation is stronger, uniform
multi-teacher weighting improves further and \methodname{} is best overall
\cite{hinton2015distillingknowledgeneuralnetwork,boizard2024towardscrosstokenizerdistillation}.
No single component fully addresses the heterogeneous distillation problem:
heterogeneous teachers provide better supervision, but the student still
needs a tokenizer-agnostic loss, sample-dependent routing and a stable
post-routing weighting scheme in the evaluated setting. The training recipe
explains this pattern because \methodname{} combines a
shared-trie Wasserstein loss for mismatched tokenizers with routed
multi-teacher selection, top-\(k\) masking, routed-weight renormalization and
EMA-smoothed gradient-agreement refinement
\cite{boizard2024towardscrosstokenizerdistillation,peyre2019computationaloptimaltransport,fedus2022switchtransformersscalingtrillion}.
The smoother DocVQA training curve in Figure~\ref{fig:training-curve-comparison}
is consistent with that design: the router narrows the teacher set first, then
GRACE reweights only the routed teachers, reducing the instability caused by
uniformly mixing heterogeneous teacher signals.
\paragraph{First-order interpretation of gradient-agreement refinement.}
The gradient-agreement component can also be understood from a first-order
multi-objective view. Let
\(g_{CE}=\nabla_\theta \mathcal{L}_{CE}\) and
\(g_i=\nabla_\theta \mathcal{L}_{KD}^{(i)}\) be the supervised and
teacher-specific KD gradients. A combined update with teacher weights \(w_i\)
is
\[
\Delta\theta
=
-\eta
\left(
g_{CE}
+
\alpha_{KD}\sum_i w_i g_i
\right).
\]
The first-order change in the supervised loss is
\[
\Delta\mathcal{L}_{CE}
\approx
g_{CE}^{\top}\Delta\theta
=
-\eta\|g_{CE}\|_2^2
-
\eta\alpha_{KD}\sum_i w_i\langle g_{CE},g_i\rangle.
\]
If \(\langle g_{CE},g_i\rangle<0\), then teacher \(i\) contributes a term that
increases the first-order CE loss instead of reducing it. This is the
gradient-conflict case studied in multi-task optimization
\cite{yu2020gradient}. GRACE does not project gradients as PCGrad does.
Instead, it uses the cosine agreement
\[
\cos(g_{CE},g_i)
=
\frac{\langle g_{CE},g_i\rangle}
{\|g_{CE}\|_2\|g_i\|_2}
\]
as a teacher-weighting signal. This explains why post-routing gradient
refinement can improve over routing alone. The largest gain appears on ChartQA
and the trend holds for SmolVLM-256M as well. Sample-dependent teacher
assignment is most useful when the task mixes visual parsing and numerical
reasoning and the method benefits both evaluated student models. The comparison
with SHM-CKA reinforces this conclusion by showing that
heterogeneous teacher access is not enough when the method relies only on
hidden-state matching instead of routed output-level KD. SHM-CKA is
competitive on ChartQA yet clearly weaker on TextVQA and DocVQA. Together with
the CKA heatmaps, this confirms that internal similarity alone does not
guarantee effective transfer. \textbf{Sample-dependent teacher weighting
therefore remains necessary, particularly when the teacher pool contains
models with different tokenizers, architectures and task-specific strengths}
\cite{kornblith2019similarityrepresentations,dasgupta2025improving}.

\section{Limitations and Threats to Validity}
\textbf{Cost-limited evaluation.}
Because the reported numbers are single-run results, the comparison does not
provide confidence intervals or repeated-seed estimates for small gaps between
strong baselines. As noted in Section~\ref{sec:efficiency-analysis}, a typical
run costs approximately 14.41 USD before offline cache generation and storage
cost. Although the main tables use a shared four-teacher pool and
consistent hyperparameters, the absence of repeated seeds limits the
strength of strict uncertainty claims about closely matched methods in this
study.

\textbf{Scope of validation.}
The empirical study focuses on VQA benchmarks and SmolVLM-family
students. Although the reported gains are strong in this regime,
the thesis does not yet establish whether the same behavior holds for other
multimodal task families, other student architectures or larger teacher pools.
